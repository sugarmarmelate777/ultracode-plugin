{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Bug Report",
  "type": "object",
  "properties": {
    "error_code": {
      "type": "string",
      "description": "The specific error code or exception name."
    },
    "severity": {
      "type": "string",
      "enum": ["critical", "high", "medium", "low"],
      "description": "Severity of the bug."
    },
    "file_path": {
      "type": "string",
      "description": "Absolute path to the file containing the bug."
    },
    "line_number": {
      "type": "integer",
      "description": "The exact line number where the bug occurs."
    },
    "reproduction_steps": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Steps to reproduce the bug."
    },
    "environment": {
      "type": "string",
      "description": "Environment where the bug was observed."
    },
    "suggested_fix": {
      "type": "string",
      "description": "The proposed code replacement or action."
    }
  },
  "required": ["error_code", "severity", "file_path", "line_number", "reproduction_steps", "environment", "suggested_fix"]
}
